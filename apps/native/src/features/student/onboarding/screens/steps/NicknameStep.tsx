/**
 * @deprecated This step is disabled and not used in the onboarding flow.
 * Nickname input has been removed from the registration process.
 */

import { View, Text } from 'react-native';

const NicknameStep = () => {
  return (
    <View>
      <Text>This step is disabled</Text>
    </View>
  );
};

export default NicknameStep;
